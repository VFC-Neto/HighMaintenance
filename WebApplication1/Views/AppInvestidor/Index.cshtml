@model IEnumerable<HighMaintenance.Models.APPXINVESTIDOR>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.APLICACOES.APP_NOME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.INVESTIDOR.INV_NOME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.APPIN_VALORINVESTIDO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.APPIN_VALORRETIRADO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.APLICACOES.APP_NOME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.INVESTIDOR.INV_NOME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.APPIN_VALORINVESTIDO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.APPIN_VALORRETIRADO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.APPIN_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.APPIN_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.APPIN_ID })
        </td>
    </tr>
}

</table>
